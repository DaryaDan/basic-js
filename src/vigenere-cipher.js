class VigenereCipheringMachine {
encrypt(message, key) {
  if (key.length == 0)
  return Error;
  if (message.length == 0)
  return Error;
}

decrypt(message, key) {
  if (key.length == 0)
  return Error;
  if (message.length == 0)
  return Error;
}

}

module.exports = VigenereCipheringMachine;
